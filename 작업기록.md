# 작업기록

이 문서는 이 저장소에서 수행한 변경 이력을 누적 기록한다.
새 작업을 할 때마다 날짜와 함께 항목을 추가한다.

## 2026-03-07

### 구조/파일 정리
- 불필요 페이지 삭제
  - `_pages/about.md`
  - `_pages/category-archive.md`
  - `_pages/tag-archive.md`
  - `_pages/year-archive.md`
- 루트 중복 워크플로 `jekyll.yml` 삭제
- 단일 페이지 포트폴리오 구조로 전환
  - `_pages/resume.md` 삭제
  - `index.md`에 이력/프로젝트/기술 스택 통합
  - `_data/navigation.yml`에서 `Resume` 메뉴 제거

### 설정/스타일 정리
- `_config.yml` 재정비
  - 사이트 메타 정보(`url`, `locale`, `timezone`) 보강
  - `author`, `social` 정보 추가
  - 깨진 인코딩/파싱 이슈 방지를 위해 파일 재작성
- `assets/css/main.scss` 정리
  - 과도한 전역 스타일 및 `!important` 제거
  - 가독성 중심의 최소 스타일 유지

### 문서화
- `README.md`를 운영 기준 문서로 전환
  - 목적/범위/수정 원칙/배포 기준 정리
  - 새 세션의 Codex가 바로 작업 맥락을 복원할 수 있게 작성

### 배포 이슈 대응
- GitHub Pages에서 404 발생 시 `_config.yml` 안정화 및 주요 페이지 재작성으로 대응
- Actions 캐시 오류(`Setup Ruby cache restore/save`)는 비치명 로그로 확인

## 2026-05-14

### 구조 개선 (이번 세션)
- 변경 파일: `_config.yml`, `index.md`, `_data/navigation.yml`
- 변경 이유: 사이트에 보이는 구조적 문제 5개 일괄 수정
- 변경 내용
  - `_config.yml`: `author.avatar` 추가 (bio-photo.jpg 연결), `author_profile: false → true`, `author.links`에 이메일 항목 추가
  - `index.md`: front matter `title`을 "연제경 — Android Developer"로 정리, 본문 h1 중복 제거, `author_profile: true` 명시
  - `_data/navigation.yml`: Experience / Projects / Skills 앵커 링크 추가
- 사용자 영향: 프로필 사진·이름·링크가 사이드바에 노출, 상단 메뉴에서 섹션 직접 이동 가능

## 2026-05-14 (UI 리뉴얼)

### 커스텀 포트폴리오 HTML 적용
- 변경 파일: `index.html` (신규), `_index_backup.md` (기존 index.md 백업)
- 변경 이유: minimal-mistakes 기본 테마 대신 Claude Design으로 제작한 커스텀 디자인 적용
- 변경 내용
  - Claude Design이 생성한 `Portfolio.html`을 `index.html`로 프로젝트 루트에 배포
  - Cloudflare 이메일 난독화 3곳을 `mailto:yllh325@gmail.com`으로 복원 (GitHub Pages 호환)
  - Cloudflare 이메일 디코드 스크립트 제거
  - 기존 `index.md`는 `_index_backup.md`로 이름 변경 (Jekyll이 무시, 빌드 충돌 방지)
- 디자인 특징: 다크/라이트 테마 토글, accent 컬러 4종 선택, 좌측 sticky 사이드바, PSR(Problem/Solution/Result) 프로젝트 카드, 스크롤 reveal 애니메이션, Pretendard + JetBrains Mono 폰트
- 사용자 영향: 전체 포트폴리오 UI가 커스텀 디자인으로 교체됨

## 2026-03-12

### 에이전트 작업 경로 보강
- 변경 파일
  - `AGENTS.md`
- 변경 이유
  - 에이전트가 저장소 작업 중 옵시디언 노트와 연계해야 할 때 참조할 고정 경로를 명시하기 위해 추가
- 사용자 영향
  - 이후 세션에서 옵시디언 볼트 경로 `C:\Users\AM11D\Documents\NockChaWang`를 별도 설명 없이 바로 참조 가능
